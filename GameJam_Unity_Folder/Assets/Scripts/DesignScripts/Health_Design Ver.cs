using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Health_DesignVer : MonoBehaviour
{
    public int Health;
    public int MaxHealth;
    // Start is called before the first frame update
    void Start()
    {
        Health = 10;
        MaxHealth = 10;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
